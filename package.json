{
  "name": "exocortex-starter-kit",
  "version": "0.0.0-dev",
  "private": true,
  "description": "Exocortex Starter Kit — ontology files + user journey assets for Obsidian vault onboarding",
  "scripts": {
    "test": "node scripts/validate-journeys.js && node --test scripts/check-invariants.unit.test.js scripts/check-invariants.test.js",
    "test:unit": "node --test scripts/check-invariants.unit.test.js",
    "test:integration": "node --test scripts/check-invariants.test.js",
    "test:journeys": "node scripts/validate-journeys.js",
    "test:invariants": "node scripts/check-invariants.js",
    "check:privacy": "bash scripts/check-privacy.sh"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@kitelev/exocortex-cli": "15.101.0"
  }
}
